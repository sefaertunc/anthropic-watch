# v1.2.0 — Schema Hardening

A minor release focused on closing documentation and contract gaps surfaced by the integration audit (`ANTHROPIC-WATCH-INTEGRATION-AUDIT.md`). This release is non-breaking: the JSON feed envelope and item shape are strictly additive (a new optional `uniqueKey` field per item). Doc prose and the README sources badge are edited to drop hardcoded source counts, but no consumer contract changes. Worclaude's production bug (dedup on `id` alone instead of `${id}|${source}`) will be fixed on their side when they adopt the new `uniqueKey` field in a future Worclaude release.

Total sources stays at **17**. This version touches schema, docs, and the feed generators — not scrapers, not sources, not the workflow.

This prompt is **plan-mode-first**. The scope spans `src/feed/`, `docs/FEED-SCHEMA.md`, fixture generation, and several doc files. Review the plan before accepting.

---

## Context

The integration audit found six documentation and contract gaps. This release addresses them in the producer (anthropic-watch). The consumer-side fixes will land in a later Worclaude release.

Audit findings addressed here:

- **R1a** (§1, HIGH) — consumers missing the `${id}|${source}` dedup contract
- **R1b** (§1, HIGH) — add pre-computed `uniqueKey` to each item so consumers can't mess it up
- **R2** (§2, HIGH) — no version-gating example in schema docs
- **R3** (§6, LOW) — no classification of fields as primary vs observability
- **R4** (§4, MEDIUM) — hardcoded source counts drift as sources are added
- **R6** (§7, LOW) — no reference fixtures consumers can pin against

Deferred or dropped from this release:

- **R5 (snippet length mismatch) — DROPPED.** Verified by inspection of all six scraper files: each emits a `snippet` field of at most 300 chars, enforced by a final `.slice(0, 300)` (see `github-releases.js:11`, `github-changelog.js:12`, `blog-page.js:82,116,174,219`, `docs-page.js:48,81,158`, `npm-registry.js:42`, `status-page.js:18–19`). The 300-char cap is correctly enforced in practice. Worclaude's 500-char truncation is defensive padding, not a response to overruns. No producer action needed.

All JSON feed changes are additive. No item id format changes. The RSS `guid` is intentionally not touched in this release (see Task 1, "File: `src/feed/rss.js`" — the fix is deferred to v2.0 to avoid a one-shot re-notification burst). The feed envelope's `"version": "1.0"` stays at 1.0 because `uniqueKey` is a new **optional** field — Worclaude and other consumers can treat it as "may be absent in archived v1.1.0 feeds" — which cleanly fits the Versioning Policy section in `docs/FEED-SCHEMA.md` ("Schema changes that add new optional fields will not bump the version").

The work is split into **two phases** to respect `CLAUDE.md` critical rule 8 (feature branches never modify shared-state files):

- **Phase 1 — feature branch `feat/v1.2.0-schema-hardening` → PR to `develop`.** Code change to `src/feed/json.js`, all `docs/FEED-SCHEMA.md` edits, fixture generation, test additions, and doc prose edits in `docs/SOURCES.md`/`docs/ARCHITECTURE.md`/`docs/WORCLAUDE-INTEGRATION.md`/etc.
- **Phase 2 — on `develop` via `/sync` after the release PR merges.** `README.md`, `package.json`, and `CHANGELOG.md` edits. These are shared-state files by project convention and land on `develop` via `/sync`, not in the feature branch.

Task numbers below carry a `[P1]` or `[P2]` tag.

> **⚠️ On the feature branch, do NOT touch `README.md`, `package.json`, or `CHANGELOG.md`.** These are shared-state files updated only on `develop` in Phase 2. If the Phase 1 PR diff touches any of them, revert those hunks before opening the PR. The branch-name CI will not catch this — it's on the implementer.

---

## Tasks

### 1. [P1] Add `uniqueKey` field to item shape (R1b)

**File: `src/feed/json.js`**

In `generateJsonFeed`, after dedup and before returning, inject `uniqueKey` on every item in the `sorted` array:

```js
const sortedWithKeys = sorted.map((item) => ({
  ...item,
  uniqueKey: `${item.id}|${item.source}`,
}));
```

Then pass `sortedWithKeys` (not `sorted`) into the `items` field of the returned JSON.

Keep the existing `const key = \`${item.id}|${item.source}\`;`dedup line as-is — it stays load-bearing for dedup;`uniqueKey` is just the same value exposed to consumers.

**File: `src/feed/rss.js` — no changes in v1.2.0.**

The RSS `guid` currently uses the raw `id` and is vulnerable to the same cross-source collision problem as any `id`-keyed dedup. Fixing it would mean emitting `guid` as `${id}|${source}`, which (per RSS 2.0 spec with `isPermaLink="false"`) is valid but causes every subscriber's RSS reader to treat every existing feed item as new on the first post-release run — across `all.xml` and each per-source feed. That blast radius is too large for a point release.

The RSS `guid` hardening is therefore **deferred to v2.0**, when the envelope `version` will bump from `"1.0"` to `"2.0"` and consumers will be explicitly prepared for schema changes. Worclaude — the primary consumer targeted by this release — reads `all.json`, not `all.xml`, so adding `uniqueKey` to JSON is sufficient to address the reported R1b bug.

### 2. [P1] Update `docs/FEED-SCHEMA.md` (R1a + R1b + R2 + R3 + R4)

Read the full file before editing. Then make the following edits. The section headings referenced below exist in the current file.

**Complete subtasks in order: 2a → 2b → 2c → 2d → 2e → 2f.** Later subtasks reference fields added by earlier ones (e.g., 2c's "Consumer Expectations" references `uniqueKey`, which 2a adds to the Field Guarantees table).

**Before editing:** there is a stray single-row markdown table near the top of the file (an orphan OPML entry floating after the main feed-files table). Leave it alone — do not try to fold it into the main table or delete it. Fixing that artifact is out of scope for this release.

**2a. Add `uniqueKey` to the "Full Example" and "Field Guarantees" table.**

In the full example, add `uniqueKey` to each item object, placed between `id` and `title`:

```jsonc
{
  "id": "v1.0.30",
  "uniqueKey": "v1.0.30|claude-code-releases", // Composite dedup key: `${id}|${source}`
  "title": "Claude Code v1.0.30",
  // ...
}
```

In the "Field Guarantees" table, add a new row between `id` and `title`. **Nullable is `Yes`** — archived v1.1.0 feeds predate this field and may not carry it; consumers should tolerate absence.

```markdown
| `uniqueKey` | `string` | Yes | — | Composite dedup key in the form `${id}|${source}`. Use this directly to deduplicate across sources. **Present since v1.2.0** — absent in archived pre-v1.2.0 feeds; consumers should fall back to `` `${item.id}|${item.source}` `` when the field is missing. |
```

**2b. Rewrite the "Programmatic Consumption" code example (R1a).**

Find the existing code example in the "Programmatic Consumption" section (currently demonstrates fetching and iterating `feed.items.slice(0, 5)`). Replace it with a more complete example that demonstrates:

1. Version gating (R2)
2. Correct composite-key dedup using `uniqueKey`
3. Persisting seen keys for the next run

Use this as the replacement:

````markdown
### Programmatic Consumption

Consumers in any language can fetch and process the feeds directly. Here is a worked example in JavaScript that demonstrates **version gating**, **composite-key deduplication**, and **state persistence** — three patterns every consumer should implement.

```js
import { readFile, writeFile } from "node:fs/promises";

const FEED_URL = "https://sefaertunc.github.io/anthropic-watch/feeds/all.json";
const STATE_PATH = "./state.json";

// Fetch the feed
const res = await fetch(FEED_URL);
const feed = await res.json();

// Version gate — fail fast on schema mismatch
if (feed.version !== "1.0") {
  throw new Error(
    `anthropic-watch feed version ${feed.version} is not supported by this consumer.`,
  );
}

// Load previously seen uniqueKeys
const prev = JSON.parse(await readFile(STATE_PATH, "utf8").catch(() => "[]"));
const seen = new Set(prev);

// Compute the composite key, falling back if the feed predates v1.2.0.
// Archived feeds from before 2026-04 won't have `uniqueKey` on items.
const keyOf = (item) => item.uniqueKey ?? `${item.id}|${item.source}`;

// Filter to genuinely new items using the composite dedup key.
// Two items with the same `id` but different `source` are distinct.
const fresh = feed.items.filter((item) => !seen.has(keyOf(item)));

console.log(`Found ${fresh.length} new items since last run.`);
for (const item of fresh) {
  console.log(`[${item.source}] ${item.title} → ${item.url}`);
}

// Persist keys for the next run.
// Note: we store the composite key, not id, so the next run's dedup is also correct.
const nextState = [...seen, ...fresh.map(keyOf)];
await writeFile(STATE_PATH, JSON.stringify(nextState, null, 2));
```

**Common pitfalls to avoid:**

- Do **not** deduplicate on `id` alone. Different sources can emit items with the same `id` (e.g. `claude-code-changelog` and `npm-claude-code` both use version strings like `"2.1.114"`). Always use `uniqueKey` or compute `${id}|${source}` yourself.
- Do **not** skip the version check. Future breaking schema changes will bump `version` from `"1.0"` to `"2.0"`; silent consumption of a new shape will surface as `undefined` field errors rather than a readable mismatch.
- Do **not** persist `id` in your state file. Persist `uniqueKey` (or `{id, source}` pairs). State files keyed only on `id` will silently collide across sources.
````

**2c. Add the "Consumer Expectations" subsection (R3).**

Insert a new `### Consumer Expectations` subsection immediately after the "Field Guarantees" table (before "Envelope Fields"). Content:

```markdown
### Consumer Expectations

Fields fall into two categories based on whether consumers should drive logic off them:

**Primary fields** — load-bearing. Consumers are expected to read these. Contract changes to these fields will be reflected in a major version bump (`version: "2.0"`).

- Item-level: `id`, `uniqueKey`, `title`, `date`, `url`, `snippet`, `source`, `sourceCategory`, `sourceName`
- Envelope-level: `version`, `items`

**Informational / observability fields** — present for debugging, rendering, and dashboards. Consumers may read these but should not depend on their exact values or presence beyond the current version.

- Envelope-level: `title`, `description`, `home_page_url`, `generator`, `ttl`, `generated`, `itemCount`
- Run-report: `runId`, `duration_ms`, `summary.*`, `sources[].durationMs`
- Run-history file: entire file is observability

Observability fields may be added, renamed, or removed across any patch release without a major version bump. Primary fields will not change without a major version bump.

**Note on nullability:** the primary vs. observability classification describes how contract changes are communicated (a major version bump for primary fields, free-to-change for observability), not whether a field is always present. A primary field may be nullable in the Field Guarantees table and may be introduced in a minor version — `uniqueKey` is the canonical example: primary (load-bearing for dedup), but `Nullable=Yes` because archived pre-v1.2.0 feeds predate it. Consumers must handle primary-but-nullable fields via the fallback documented on the field's row.
```

**2d. Add the source count variability warning (R4).**

Find the OPML section (currently says something like "Import this file into any RSS reader to subscribe to all 17 feeds at once"). **Replace that single existing sentence entirely** with the following paragraph — do not keep the original line alongside the replacement.

```markdown
Import this file into any RSS reader to subscribe to every source at once. The exact number of sources is not stable — sources may be added or removed over time. Consumers must derive counts from `summary.sourcesChecked` or `sources.length` in `run-report.json`, never hardcode a number. The `sources.opml` file regenerates each run from `src/sources.js`, so the outline always matches the current source list.
```

Also search the document for any other hardcoded source count (phrases like "17 sources", "all 17", "monitors 17") and replace with non-numeric phrasing. Do not introduce new numbers.

**2e. Update the "Merge Semantics" section (minor).**

The section currently says:

> **Dedup key:** `${id}|${source}` — two items with the same id and source are considered the same item.

Keep this line, but add a cross-reference to the new `uniqueKey` field:

```markdown
**Dedup key:** `${id}|${source}` — two items with the same id and source are considered the same item. The `uniqueKey` field on each item (added in v1.2.0) contains this pre-composed value so consumers can dedupe directly without string concatenation.
```

**2f. Add a forward-looking note for RSS `guid` consumers.**

Add a short paragraph in the "RSS 2.0 Feed" (or equivalent RSS) section of `docs/FEED-SCHEMA.md`, clearly marked as a planned change. Recommended wording:

```markdown
> **Planned for v2.0:** The RSS `guid` element currently uses the bare item `id`, which can collide across sources. In v2.0 the `guid` will change to the composite `${id}|${source}` form (matching the JSON `uniqueKey` field added in v1.2.0). Because RSS readers dedupe on `guid`, this change will cause a one-time re-notification of every existing feed item across `all.xml` and each per-source feed, and will therefore be released alongside the envelope `version` bump from `"1.0"` to `"2.0"`. RSS consumers who need collision-free identifiers today should compute `${id}|${source}` from the parsed feed manually or switch to `all.json` and use the `uniqueKey` field.
```

### 3. [P1] Ship reference fixtures (R6)

**Create directory: `docs/fixtures/`**

Capture two fixtures from a sandboxed pipeline run — **do not touch the real `state/last-seen.json` or `public/feeds/`**. The pipeline's `runPipeline` entry point accepts `stateDir` and `feedsDir` options (see `src/index.js` — verify the current signature before running). Use a temporary directory so the run is side-effect-free.

1. With `GITHUB_TOKEN` set, run the pipeline against throwaway directories:

   ```bash
   FIXTURE_DIR=$(mktemp -d)
   node --input-type=module -e "
     import('./src/index.js')
       .then(m => m.runPipeline({ stateDir: '$FIXTURE_DIR/state', feedsDir: '$FIXTURE_DIR/feeds' }))
       .catch(e => { console.error(e); process.exit(1); });
   "
   ```

   The run writes `$FIXTURE_DIR/feeds/all.json` and `$FIXTURE_DIR/feeds/run-report.json` reflecting the new `uniqueKey` field. Real `state/last-seen.json` is never read or written; real `public/feeds/` is never touched.

   Because `$FIXTURE_DIR/state` is empty, every source emits its current items as new — `all.json` will comfortably exceed 10 items, so the zero-item edge case doesn't apply.

2. Copy the outputs to fixture locations:

   ```bash
   cp "$FIXTURE_DIR/feeds/all.json" docs/fixtures/all.sample.json
   cp "$FIXTURE_DIR/feeds/run-report.json" docs/fixtures/run-report.sample.json
   ```

3. Trim `docs/fixtures/all.sample.json` to at most 10 items (one per source category if possible, or whatever the first 10 happen to be) to keep the file readable. **After trimming, update the envelope's `itemCount` field to match the trimmed `items.length`** so the fixture is internally consistent — a consumer running a contract test on the fixture would otherwise see `itemCount` disagree with `items.length`. Preserve all other envelope fields exactly as generated — do not synthesize or hand-edit any other field values.

4. If for any reason synthetic items are introduced, do **not** annotate them inside the fixture JSON (no schema field allows this, and comment-style annotations would break the contract). Document synthetic status only in the provenance README described in step 5. Do not use "archived feed snapshots from git history" — `public/feeds/*` is gitignored and isn't present on `main`.

5. Create `docs/fixtures/README.md` documenting each fixture's provenance. Use this template per fixture:

   ```markdown
   ## all.sample.json

   - Captured: <YYYY-MM-DD>
   - Pipeline version: 1.2.0
   - Source: live fresh-state run via `runPipeline({stateDir,feedsDir})`
   - Items: <N> trimmed from the captured <M>; `itemCount` updated to <N>
   - Notes: <any deviations from live output, e.g. "items[3].snippet hand-truncated to 200 chars">

   ## run-report.sample.json

   - Captured: <YYYY-MM-DD>
   - Pipeline version: 1.2.0
   - Source: same run as all.sample.json
   - Not trimmed
   ```

   The README is the only place synthetic status or any deviation from live output may be disclosed without breaking the JSON contract.

**Add a note to `docs/FEED-SCHEMA.md`:**

Add a new subsection **immediately before the "Versioning Policy" section** (so Versioning Policy remains the last section of the file). Do not place it at the true bottom of the file — Versioning Policy must stay last.

```markdown
### Reference Fixtures

Consumers can pin against reference fixtures shipped in this repo for contract testing:

- `docs/fixtures/all.sample.json` — sample `all.json` response conforming to this schema
- `docs/fixtures/run-report.sample.json` — sample `run-report.json` response

These fixtures are regenerated each release and reflect the current schema version. Fixtures update additively — fields may be added across releases. Contract tests should assert on fields they care about rather than on the full shape.
```

### 4. [P2] Update `README.md`

Performed on `develop` after the Phase 1 PR merges. The README is shared-state per `CLAUDE.md` rule 8; it does not belong in the feature branch.

Six edits — sweep all hardcoded `17` references:

**a.** Bump the version badge from `version-v1.1.0-blue` to `version-v1.2.0-blue` (currently at `README.md:5`).

**b.** Change the sources badge (currently at `README.md:7`) from a hardcoded count to a non-numeric label. The actual current URL is `https://img.shields.io/badge/sources-17%20monitored-blue`; replace it with `https://img.shields.io/badge/sources-monitored-blue` (alt text `"sources: monitored"`). The badge stays — only the stale number goes.

**c.** The opening tagline at `README.md:24` ("Monitors 17 Anthropic sources daily for changes and publishes structured feeds") — change to "Monitors Anthropic sources daily for changes and publishes structured feeds." Source count is variable and shouldn't be in the tagline.

**d.** Sweep the remaining four `17` references in the README body — verified at lines 96, 100, 142, and 157 at time of planning (line numbers may drift slightly; grep `README.md` for `\b17\b` and review each match). Soften each:

- `README.md:96` — "The pipeline fetches all 17 sources..." → drop "all 17".
- `README.md:100` — "→ Fetch 17 sources..." inside the ascii diagram → drop "17".
- `README.md:142` — "sources.js # 17 source definitions" in the project-structure block → drop "17".
- `README.md:157` — "all 17 sources with detection methods..." → drop "all 17".

**e.** Under "What is this?", verify no hardcoded "17 sources" remains after steps a–d. If any does, soften.

**f.** Run `grep -n '\b17\b' README.md` after the edits — the only remaining occurrence should be inside the CHANGELOG date (if referenced) or verifiable SPEC references. Anything else is drift.

### 5. [P2] Update `package.json`

Performed on `develop` after the Phase 1 PR merges. `package.json` version is shared-state per `CLAUDE.md` rule 8.

Bump `"version"` from `"1.1.0"` to `"1.2.0"`. User-Agent auto-follows.

### 6. [P2] Update `CHANGELOG.md`

Performed on `develop` after the Phase 1 PR merges. `CHANGELOG.md` is shared-state per `CLAUDE.md` rule 8.

This entry introduces a `Deferred` section, which is a first in this project's CHANGELOG (prior sections: Added, Changed, Fixed, Security, Workflow, Docs, Migration). No linter enforces the section list; the addition is intentional to make the RSS-guid deferral visible.

Prepend a new entry above `[1.1.0]`:

```markdown
## [1.2.0] - <YYYY-MM-DD>

A minor release addressing schema documentation and contract gaps surfaced by the integration audit of Worclaude's `anthropic-watch` consumer. JSON feed changes are strictly additive — non-breaking for existing consumers. The feed envelope `version` stays `"1.0"` because the new `uniqueKey` field is optional (may be absent in archived pre-v1.2.0 feeds), fitting the Versioning Policy's "new optional fields" exemption.

### Added

- **`uniqueKey` field on every JSON feed item** — pre-computed `${id}|${source}` composite key. Consumers can now deduplicate directly using this field instead of reconstructing the composite key themselves. Prevents the class of bug where consumers dedupe on `id` alone and collide across sources. This is additive; existing consumers are unaffected.
- **Reference fixtures** at `docs/fixtures/all.sample.json` and `docs/fixtures/run-report.sample.json`, with provenance documented in `docs/fixtures/README.md`. Consumers can pin against these for contract testing.
- **"Consumer Expectations" subsection** in `docs/FEED-SCHEMA.md` classifying fields as primary (load-bearing, version-bumped on change) vs observability (may change freely).

### Changed

- **`docs/FEED-SCHEMA.md` "Programmatic Consumption" example rewritten** to demonstrate version gating, composite-key dedup, and state persistence. Previous example showed only a slice of items; new example is a full consumer template.
- **Hardcoded source counts removed from README body copy and prose docs** (`docs/FEED-SCHEMA.md` OPML section, `docs/SOURCES.md`, `docs/ARCHITECTURE.md`). The README sources badge was also updated from a numeric count to a non-numeric `sources-monitored` label. Consumers should derive counts from `summary.sourcesChecked` or `sources.length` in `run-report.json`.
- `package.json` version bumped to `1.2.0`.

### Deferred

- **RSS `guid` composite-key change deferred to v2.0.** Switching `guid` from the bare `id` to `${id}|${source}` in a point release would cause every RSS reader to treat every existing feed item as new on the first post-release sync, across `all.xml` and each per-source feed. That blast radius is inappropriate for a point release. The change is batched with the next envelope `version` bump to `"2.0"`, where consumers will be explicitly prepared for schema changes. A forward-looking note now exists in `docs/FEED-SCHEMA.md`.

### Docs

- `docs/FEED-SCHEMA.md` — new `uniqueKey` field documented, new "Consumer Expectations" subsection, rewritten "Programmatic Consumption" example, new "Reference Fixtures" subsection (placed immediately before "Versioning Policy"), source count warning, and a forward-looking note that RSS `guid` will change to composite form in v2.0.

### Migration

No consumer-side migration is required for v1.2.0. Existing consumers that dedupe on `id` alone continue to work (same bug they had before), but are encouraged to switch to `uniqueKey`-based dedup. RSS consumers are unaffected in this release — `guid` output is unchanged. RSS `guid` is planned to change to `${id}|${source}` in a future v2.0 release, at which point the envelope `version` will bump to `"2.0"` and a one-time re-notification of feed items across RSS readers is expected; that change is deliberately deferred to batch with the envelope version bump.
```

Fill in the actual date.

### 7. [P1] Update other docs

Prose-only edits — these land in the Phase 1 feature branch.

**`docs/SOURCES.md`**: No source-list changes. Verify hardcoded counts elsewhere in the file and soften if appropriate (e.g., "This project monitors 17 sources" → "This project monitors several Anthropic sources across six scraper types"). The scraper-type count ("six") is acceptable because it corresponds to the files in `src/scrapers/` and is stable; the source count is what varies. Do not reduce information density — just remove the number.

**`docs/ARCHITECTURE.md`**: Same pass — remove hardcoded counts, reference the dynamic source count from `run-report.json`.

**`docs/ADDING-SOURCES.md`**: No change expected. Skim for "17 sources" references; remove if any.

**`docs/WORCLAUDE-INTEGRATION.md`**: This file is directly relevant — add a note at the top mentioning the audit recommendations now reflected in v1.2.0, and point to the updated `FEED-SCHEMA.md` consumption example.

**`docs/TROUBLESHOOTING.md`**: Skim for count references.

**`test/capture-fixtures.js`**: The file-level comment at line 4 currently reads "Fetches real responses from all 17 sources...". Soften to drop the `17`. This is a dev-only helper, low-impact, but keeps the "no hardcoded source counts" sweep consistent.

**`docs/spec/SPEC.md`**: Intentionally untouched. Both line 7 ("as of v1.1.0 it is 17 sources") and line 114 (Phase 3 bullet "api-sdk-py-releases added (17 sources total)") are preserved as point-in-time historical records of v1.1.0 state. `/sync` will update SPEC.md on its own cadence after a release, not as part of this task.

### 8. [P1] Tests

Add tests for the new `uniqueKey` field. Minimally:

- Test in `test/unit/feed-json.test.js` (match the existing suite's path — files under `test/unit/`) that `generateJsonFeed` output has `uniqueKey` on every item and that its value is `${item.id}|${item.source}`.
- Test that two items with the same `id` but different `source` both appear in the output and have distinct `uniqueKey` values.

**No RSS test changes are needed in v1.2.0.** The RSS output is unchanged this release; `test/unit/feed-rss.test.js` and `test/helpers/rss-validator.js` should keep asserting the existing `guid` behavior (bare `id`). When the v2.0 RSS `guid` change lands, that is where the test updates belong.

Before committing, open `test/e2e/feed-validation.test.js` and confirm whether it contains any `guid`-specific assertions. If it does, they should continue to assert the unchanged pre-v1.2.0 behavior (bare `id`); only the JSON side gains a new assertion.

The project convention (`.claude/rules/testing.md`) is explicit assertions over snapshot tests; there are no existing snapshot tests on JSON feed output. Do not introduce one — write explicit assertions.

Run `npm test` after all changes. All tests must pass.

### 9. Verify and dry-run

**Phase 1 verification (feature branch, before opening the PR to develop):**

1. `npm test` — all pass.
2. Run the sandboxed pipeline from Task 3 step 1 (against `$FIXTURE_DIR`, not real state). `$FIXTURE_DIR/feeds/all.json` must contain `uniqueKey` on every item. Do not run `node src/cli.js` — it writes to real `state/` and real `public/feeds/` and is unnecessary for verification.
3. Verify at least one item's `uniqueKey` matches the expected `${id}|${source}` format.
4. Verify `$FIXTURE_DIR/feeds/all.xml` `guid` elements match the pre-v1.2.0 bare-`id` behavior in `src/feed/rss.js` — the RSS change is deferred to v2.0, so any drift here is a bug. Concrete assertion: parse the XML with `fast-xml-parser` and check that the first item's `guid` text equals the first item's `id` in the JSON feed verbatim (not the composite `${id}|${source}`). Example:

   ```bash
   node --input-type=module -e "
     import('node:fs').then(async (fs) => {
       const { XMLParser } = await import('fast-xml-parser');
       const xml = fs.readFileSync('$FIXTURE_DIR/feeds/all.xml', 'utf8');
       const json = JSON.parse(fs.readFileSync('$FIXTURE_DIR/feeds/all.json', 'utf8'));
       const parsed = new XMLParser({ ignoreAttributes: false }).parse(xml);
       const rssGuid = parsed.rss.channel.item[0].guid['#text'] ?? parsed.rss.channel.item[0].guid;
       const jsonId = json.items[0].id;
       if (rssGuid !== jsonId) { console.error('FAIL: guid=' + rssGuid + ' id=' + jsonId); process.exit(1); }
       if (rssGuid.includes('|')) { console.error('FAIL: guid is composite, RSS change leaked'); process.exit(1); }
       console.log('OK: RSS guid is bare id (' + rssGuid + ')');
     });
   "
   ```

5. Verify `docs/fixtures/all.sample.json`, `docs/fixtures/run-report.sample.json`, and `docs/fixtures/README.md` exist. Confirm `all.sample.json` has `itemCount` matching `items.length` (after trimming).

**Phase 2 verification (after release PR merges to `main`, before tagging):**

6. `git log origin/main..main` — output must be empty. If non-empty, the local `main` has diverged from origin and the tag must not be pushed until that's reconciled.
7. `grep -n '\b17\b' README.md` — output must contain no stale source-count references in body/badge/tagline (CHANGELOG date lines are fine).
8. `cat package.json | jq -r .version` prints `1.2.0`.
9. `head -30 CHANGELOG.md` shows the new `[1.2.0]` block at the top with the `Deferred` section present.

---

## File modification checklist

### Phase 1 — feature branch `feat/v1.2.0-schema-hardening`

- [ ] `src/feed/json.js` — inject `uniqueKey` on each item
- [ ] `docs/FEED-SCHEMA.md` — six additions (uniqueKey row with `Nullable=Yes`, Consumer Expectations, Programmatic Consumption rewrite with `uniqueKey ?? composite` fallback, Reference Fixtures [placed before Versioning Policy], source count warning, v2.0 RSS `guid` deferral note)
- [ ] `docs/fixtures/all.sample.json` — new file (trimmed items; `itemCount` updated to match trimmed length)
- [ ] `docs/fixtures/run-report.sample.json` — new file
- [ ] `docs/fixtures/README.md` — new file documenting each fixture's provenance (per-fixture template)
- [ ] Test files in `test/unit/` — new `uniqueKey` test; no RSS test changes; no snapshot tests
- [ ] `docs/SOURCES.md`, `docs/ARCHITECTURE.md`, `docs/WORCLAUDE-INTEGRATION.md`, `docs/ADDING-SOURCES.md`, `docs/TROUBLESHOOTING.md` — remove hardcoded counts where found
- [ ] `test/capture-fixtures.js` — soften the "17 sources" comment at line 4

### Phase 2 — on `develop` after Phase 1 PR merges

Manual commit on `develop` before running `/sync`:

- [ ] `README.md` — version badge bump; sources badge URL changed to `https://img.shields.io/badge/sources-monitored-blue`; tagline drops "17"; sweep README body for remaining `17` references at approx lines 96, 100, 142, 157
- [ ] `CHANGELOG.md` — new `[1.2.0]` entry at top (includes first-use `Deferred` section noting RSS guid)

Applied automatically by `/sync`:

- [ ] `package.json` — version bump to `1.2.0` (handled by `/sync`; do not bump manually)
- [ ] `docs/spec/PROGRESS.md`, `docs/spec/SPEC.md` — updated by `/sync` as part of its normal flow

### Must NOT be touched (either phase)

- `src/feed/rss.js`, `src/feed/opml.js` — RSS `guid` change deferred to v2.0; OPML generator already dynamic. No changes in this release.
- `src/sources.js` — no sources change
- `src/scrapers/*` — no scraper changes
- `src/state.js`, `src/fetch-with-retry.js`, `src/fetch-source.js`, `src/index.js` — no pipeline changes
- `.github/workflows/*` — no workflow changes
- `docs/spec/SPEC.md` — intentionally left as-is. Line 7 ("as of v1.1.0 it is 17 sources") and line 114 (Phase 3 bullet "api-sdk-py-releases added (17 sources total)") are both preserved as point-in-time historical records. `/sync` handles SPEC.md on its own cadence.
- `CONTRIBUTING.md`, `CODE_OF_CONDUCT.md`, `SECURITY.md`, `LICENSE` — community files untouched
- `public/feeds/*` — regenerated at runtime, never hand-edited; sandboxed fixture generation in Task 3 uses a throwaway `$FIXTURE_DIR`, not `public/feeds/`
- `state/last-seen.json` — never read or written during this task. Task 3 uses `runPipeline({stateDir, feedsDir})` with throwaway dirs precisely to protect this file.

---

## After completion

Follow the documented `develop → main` release flow. **Never push directly to `main`** (enforced by `CLAUDE.md` critical rule and by `.github/workflows/branch-name-check.yml`).

Phase mechanics reflect what `/sync` (see `.claude/commands/sync.md`) actually does:

- `/sync` updates **`PROGRESS.md`**, **`SPEC.md`**, and **`package.json` version** on `develop`.
- `/sync` commits those changes with its own fixed message (`chore: sync progress, spec, and version to <new version>`), pushes `develop`, and opens the release PR `develop → main` itself.
- `/sync` does **not** touch `README.md` or `CHANGELOG.md`. Those edits must be applied manually on `develop` **before** `/sync` runs, as a separate commit.

### Phase 1 — feature branch PR

1. Create a feature branch from `develop`:
   ```
   git checkout develop
   git pull
   git checkout -b feat/v1.2.0-schema-hardening
   ```
2. Implement Tasks 1, 2, 3, 7, 8. **Do not touch `README.md`, `package.json`, or `CHANGELOG.md` on this branch.**
3. Commit: `feat: add uniqueKey field and schema docs hardening`.
4. Open PR `feat/v1.2.0-schema-hardening → develop`. Verify CI runs the Phase 1 verification checks (Task 9 steps 1–5). Merge when green.

### Phase 2 — shared-state edits on develop, then `/sync`

**Precondition:** CI on `develop` must be green after the Phase 1 merge. Do not proceed to Phase 2 if CI is red — fix it first.

5. Update local `develop` and audit drift:

   ```
   git checkout develop
   git pull
   git log origin/main..develop --oneline
   ```

   Review any unexpected commits between `origin/main` and `develop`. If other PRs landed during Phase 1, either widen the CHANGELOG scope to cover them, rebase, or coordinate with the author. Do not silently let unrelated work ride in on v1.2.0.

6. Apply Tasks 4 and 6 manually (README + CHANGELOG — the two shared-state files `/sync` won't touch). Do **not** bump `package.json` — `/sync` will do that in step 7. Commit and push:

   ```
   git add README.md CHANGELOG.md
   git commit -m "chore: release v1.2.0 — bump version and update docs"
   git push origin develop
   ```

7. Run `/sync`. It will:
   - Update `PROGRESS.md` and `SPEC.md`.
   - Bump `package.json` to `1.2.0` (Task 5).
   - Run `/verify` mid-flow.
   - Commit as `chore: sync progress, spec, and version to 1.2.0`.
   - Push `develop`.
   - Open the release PR `develop → main` automatically.

   **If `/sync` proposes a version other than `1.2.0`, or if `/verify` fails, stop and reconcile before proceeding.** `/sync` infers the version bump from `.claude/skills/git-conventions/SKILL.md`'s Versioning Policy applied to merged PRs — it does not read `CHANGELOG.md`. A mismatch here means the CHANGELOG you just wrote and `/sync`'s inference disagree on the release type; resolve before letting `/sync` commit and push.

### Phase 3 — verify and merge the release PR, then tag

8. Open the release PR `/sync` created. Verify it contains both your Phase 2 commit (README + CHANGELOG) and `/sync`'s own commit (PROGRESS + SPEC + package.json). CI must be green. Merge.
9. On `main` after merge, confirm the tag is safe to push (Task 9 step 6):
   ```
   git checkout main
   git pull
   git log origin/main..main      # must be empty
   ```
10. Tag and push the tag only (not the branch — `main` is already pushed by the PR merge):
    ```
    git tag v1.2.0
    git push origin v1.2.0
    ```
11. Create the GitHub Release with the v1.2.0 CHANGELOG entry as the body.
12. Back-merge `main` into `develop` (`/sync`'s back-merge path, or `git checkout develop && git merge --no-ff main && git push`) so `develop` stays in sync with the release tag.
13. The next scheduled cron run will emit feeds with the new `uniqueKey` field on JSON items. RSS output (`all.xml`, per-source feeds) is unchanged — the `guid` change is held for v2.0.

## Downstream consumer note

Worclaude's consumer (`scripts/upstream-precheck.mjs`) currently dedupes on `id` alone — this is the live bug documented in `ANTHROPIC-WATCH-INTEGRATION-AUDIT.md` §1. The consumer will continue to work (same buggy behavior as before) until Worclaude is updated to use `uniqueKey`. That update is tracked as a separate Worclaude release; it is not a blocker for this anthropic-watch v1.2.0 release.
