name: Branch name check

on:
  pull_request:
    branches: [main]
    types: [opened, reopened, synchronize, edited]

jobs:
  validate-branch-name:
    name: Validate branch name
    runs-on: ubuntu-latest
    steps:
      - name: Check source branch name
        run: |
          BRANCH="${{ github.head_ref }}"
          echo "Source branch: $BRANCH"

          # Allow the documented release source (develop → main)
          if [[ "$BRANCH" == "develop" ]]; then
            echo "✅ Release source branch — skipping check."
            exit 0
          fi

          # Allow automated dependency PRs
          if [[ "$BRANCH" =~ ^(dependabot|renovate)/ ]]; then
            echo "✅ Automated dependency branch — skipping check."
            exit 0
          fi

          # Enforce feat/* convention for everything else
          if [[ ! "$BRANCH" =~ ^feat/.+ ]]; then
            echo "❌ PRs to main must come from 'develop' (release) or 'feat/<something>' (direct feature)."
            echo "   Got: '$BRANCH'"
            exit 1
          fi

          echo "✅ Branch name OK."
